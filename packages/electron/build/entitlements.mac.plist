<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Required for Electron and V8 -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    <!-- Allow spawning subprocess (for Claude Code MCP) -->
    <key>com.apple.security.inherit</key>
    <true/>
    <!-- File access -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
    <!-- Network access -->
    <key>com.apple.security.network.client</key>
    <true/>
    <key>com.apple.security.network.server</key>
    <true/>
    <!-- Microphone: required so Voice Mode can request access via -->
    <!-- askForMediaAccess(). Every non-voice session is denied audio by the -->
    <!-- microphone gate (src/main/mediaPermissionGate.ts), so the only prompt -->
    <!-- a user sees is on explicit Voice Mode activation (GitHub #22 / NIM-1535). -->
    <key>com.apple.security.device.audio-input</key>
    <true/>
</dict>
</plist>